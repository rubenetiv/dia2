import math

# a) Función suma de dos números
def add_two_numbers(a, b):
    return a + b

# b) Área del círculo
def area_del_circulo(radio):
    return math.pi * radio ** 2

# c) Suma arbitraria con validación de números
def add_all_nums(*args):
    total = 0
    for num in args:
        if not isinstance(num, (int, float)):
            return "Error: Todos los elementos deben ser numéricos"
        total += num
    return total

# d) Conversión Celsius a Fahrenheit
def convert_celsius_to_fahrenheit(celsius):
    return (celsius * 9/5) + 32

# e) Solución de ecuación cuadrática
def solve_quadratic_eqn(a, b, c):
    discriminante = b**2 - 4*a*c
    if discriminante < 0:
        return "No tiene soluciones reales"
    elif discriminante == 0:
        x = -b / (2*a)
        return (x,)
    else:
        x1 = (-b + math.sqrt(discriminante)) / (2*a)
        x2 = (-b - math.sqrt(discriminante)) / (2*a)
        return (x1, x2)

# f) Imprimir lista elemento por elemento
def print_list(lista):
    for elemento in lista:
        print(elemento)

# g) Devolver lista invertida usando bucles
def lista_inversa(lista):
    inversa = []
    for i in range(len(lista)-1, -1, -1):
        inversa.append(lista[i])
    return inversa

# --- Ejemplos de uso (puedes probar estos) ---

# print(add_two_numbers(3, 5))  
# print(area_del_circulo(10))  
# print(add_all_nums(1, 2, 3, 4))  
# print(add_all_nums(1, "a", 3))  
# print(convert_celsius_to_fahrenheit(0))  
# print(solve_quadratic_eqn(1, -3, 2))  
# print_list(["Python", "Java", "C++"])  
# print(lista_inversa([1, 2, 3, 4, 5]))  
